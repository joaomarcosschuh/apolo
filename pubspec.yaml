name: my_medical_study_app
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Firebase packages
  firebase_core: ^2.13.0
  firebase_auth: ^4.6.1
  cloud_firestore: ^4.7.1

  # Other packages
  # Add here other packages you need for your project

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
